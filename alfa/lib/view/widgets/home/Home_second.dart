import 'package:flutter/material.dart';

class Home_second extends StatelessWidget {
  const Home_second({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 900,
    );
  }
}
